# CMC Configuration
CMC_URL=https://your-cluster.cloudstaging.incortalabs.com/cmc
CMC_USER=admin
CMC_PASSWORD=your-password
CMC_CLUSTER_NAME=customCluster #example

# Optional
VERIFY_SSL=true

# Incorta Configuration (for Zendesk/Jira queries)
INCORTA_ENV_URL=https://your-incorta-env.com
INCORTA_TENANT=your-tenant
INCORTA_USERNAME=your-username
INCORTA_PASSWORD=your-password

# SSL Verification (optional, defaults to true)
VERIFY_SSL=true

QDRANT_URL=
QDRANT_API_KEY=
# Incorta credentials
INCORTA_ENV_URL="/incorta"
INCORTA_TENANT=""
INCORTA_SQLX_HOST="jdbc:hive2://my-incorta-prod-sqlx.spark-cloud.incorta.com:443/;transportMode=http;httpPath=gc-us-central1-f-50/cliservice;ssl=true"
INCORTA_USERNAME="h"
INCORTA_PASSWORD=""

# Cloud Portal Configuration
# Token is obtained automatically via browser OAuth login (no config needed for staging).
# Static token fallback (optional — if browser login is not available):
# CLOUD_PORTAL_TOKEN=
# CLOUD_PORTAL_USER_ID=
# Auth0 overrides (optional — defaults work for staging):
# AUTH0_DOMAIN=auth-staging.incortalabs.com
# AUTH0_CLIENT_ID=1H6oWlDKORKc6BmiYWSjECS8Zq6XesV8
# AUTH0_AUDIENCE=https://cloud.server/api/
